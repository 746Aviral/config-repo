server:
  port: 9001  # Change this port if needed

# spring:
#   kafka:
#     bootstrap-servers: localhost:9092
#     topic:
#       notification: notification-topic
#     consumer:
#       group-id: notification-group
#       auto-offset-reset: earliest
#       enable-auto-commit: false
#       key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#       value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
#       properties:
#         spring.json.trusted.packages: "com.blueyonder.bankingapp.model"

#     producer:
#       key-serializer: org.apache.kafka.common.serialization.StringSerializer
#       value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

  # mail:
  #   host: smtp.gmail.com
  #   port: 587
  #   username: interview0241@gmail.com  # Use env variables instead of hardcoding credentials
  #   password: jbkl psqx vjzu abzj  # Secure this in environment variables or Vault
  #   protocol: smtp
  #   tls: true
  #   properties:
  #     mail:
  #       smtp:
  #         auth: true
  #         starttls:
  #           enable: true
  #         ssl:
  #           trust: smtp.gmail.com

  datasource:
    url: jdbc:postgresql://localhost:5432/bankingapp
    username: postgres   # Use environment variables
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    root: INFO
    com.blueyonder.bankingapp: DEBUG

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
